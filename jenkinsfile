pipeline {
    agent any

    environment {
        GIT_REPO = 'https://github.com/mariemkahli1/DevopsProject.git'
        WORKSPACE_DIR = '/var/lib/jenkins/workspace/deploy_note_app'
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: "${GIT_REPO}"
            }
        }

        stage('Copy Files') {
            steps {
                script {
                    // Copie les fichiers du workspace cloné dans le répertoire de destination
                    sh "cp -r * ${WORKSPACE_DIR}/"
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed.'
        }
    }
}
